__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# personal runtime data (bet log + CLV), created by track.py on first use
data/bets.json
.vercel

# autonomous refresh runtime state (regenerated on each run)
data/.auto_refresh_state.json

# The Odds API: secret key must never be committed; odds cache is regenerated
data/odds_api_key.txt
data/.odds_state.json
data/kickoffs.json
